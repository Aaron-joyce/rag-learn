# rag-learn

Agentic Retrieval-Augmented Generation demo — an LLM (Gemini) decides when to call retrieval tools against a local ChromaDB knowledge base, rather than retrieving on every query by default.

See [`rag-demo/`](./rag-demo) for the full write-up, architecture, and setup instructions.
